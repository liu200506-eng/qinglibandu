import os
from pathlib import Path

documents_dir = Path(__file__).parent / 'knowledge_base' / 'computer_network' / 'documents'

docs_to_generate = {
    'IP地址与子网划分.md': '''# IP地址与子网划分

## IP地址概述

IP地址是互联网中设备的唯一标识，用于在网络中定位和通信。IPv4地址由32位二进制数组成，通常表示为点分十进制形式（如192.168.1.1）。

### IP地址分类

IPv4地址根据第一个字节分为五类：

- **A类地址**：1-126，用于大型网络
- **B类地址**：128-191，用于中型网络
- **C类地址**：192-223，用于小型网络
- **D类地址**：224-239，用于组播
- **E类地址**：240-255，保留用于实验

### 子网划分

子网划分是将一个大的IP网络分成多个小网络的过程，通过子网掩码实现。

**子网掩码**：用于区分网络地址和主机地址，由连续的1和0组成。

**CIDR表示法**：使用斜杠加数字表示子网掩码中1的个数，如192.168.1.0/24。

### 子网划分示例

假设拥有一个C类网络192.168.1.0/24，需要划分成4个子网：

1. 确定需要的子网位数：2^n >= 4，所以n=2
2. 子网掩码变为/26（24+2）
3. 每个子网有64个地址（2^6），其中可用主机数为62个（减去网络地址和广播地址）

**VLSM（可变长子网掩码）**：允许在同一网络中使用不同长度的子网掩码，提高地址利用率。

### IPv6简介

IPv6地址由128位组成，采用冒号分隔的十六进制表示，如2001:0db8:85a3:0000:0000:8a2e:0370:7334。IPv6解决了IPv4地址枯竭的问题，并提供更好的安全性和性能。

## 总结

IP地址与子网划分是网络层的核心概念，掌握这些知识对于设计和管理网络至关重要。
''',

    '路由算法.md': '''# 路由算法

## 路由概述

路由是指将数据包从源主机转发到目标主机的过程。路由算法是路由器用来决定最佳路径的规则和方法。

### 路由算法分类

1. **静态路由**：由管理员手动配置，适用于小型网络
2. **动态路由**：路由器自动学习网络拓扑并更新路由表

### 距离向量算法

距离向量算法基于Bellman-Ford算法，路由器定期向邻居发送自己的路由表。

**特点**：
- 每个路由器只知道到邻居的距离
- 收敛速度较慢
- 容易产生计数到无穷问题

**解决方法**：
- 水平分割：不向来源方向发送路由信息
- 毒性逆转：将来源方向的路由度量设为无穷大

### 链路状态算法

链路状态算法基于Dijkstra算法，每个路由器维护完整的网络拓扑图。

**特点**：
- 每个路由器知道整个网络的拓扑
- 收敛速度快
- 计算复杂度较高

**工作步骤**：
1. 发现邻居并建立邻接关系
2. 测量到邻居的延迟
3. 向全网广播链路状态信息
4. 构建网络拓扑图
5. 使用Dijkstra算法计算最短路径

### 路径向量算法

路径向量算法结合了距离向量和链路状态的优点，常用于BGP协议。

**特点**：
- 记录到达目的地的完整路径
- 可以避免路由环路
- 支持策略路由

## 总结

路由算法是网络层的核心技术，不同算法适用于不同规模和需求的网络。
''',

    'RIP与OSPF.md': '''# RIP与OSPF

## RIP协议

RIP（Routing Information Protocol）是一种距离向量路由协议。

### RIP特点

- 使用跳数作为度量标准，最大跳数为15
- 默认每30秒发送一次路由更新
- 支持水平分割和毒性逆转
- 适用于小型网络

### RIP版本

- **RIPv1**：不支持VLSM，使用广播发送更新
- **RIPv2**：支持VLSM，使用组播发送更新，支持认证

### RIP工作过程

1. 路由器启动时，初始化路由表
2. 定期向邻居发送完整路由表
3. 接收邻居的路由更新
4. 使用Bellman-Ford算法更新路由表

## OSPF协议

OSPF（Open Shortest Path First）是一种链路状态路由协议。

### OSPF特点

- 使用带宽作为度量标准
- 支持大型网络，最多可支持数千个路由器
- 收敛速度快
- 支持VLSM和CIDR
- 使用组播发送更新

### OSPF区域

为了支持大型网络，OSPF将网络划分为多个区域：

- **骨干区域（Area 0）**：必须存在，连接其他区域
- **非骨干区域**：连接到骨干区域

### OSPF工作过程

1. 建立邻居关系
2. 交换链路状态信息（LSA）
3. 构建链路状态数据库（LSDB）
4. 使用Dijkstra算法计算最短路径
5. 生成路由表

### RIP与OSPF对比

| 特性 | RIP | OSPF |
|------|-----|------|
| 算法类型 | 距离向量 | 链路状态 |
| 度量标准 | 跳数 | 带宽 |
| 最大跳数 | 15 | 无限制 |
| 收敛速度 | 慢 | 快 |
| 适用网络 | 小型 | 大型 |

## 总结

RIP适用于小型网络，配置简单；OSPF适用于大型网络，功能强大但配置复杂。
''',

    'ICMP与ping.md': '''# ICMP与ping

## ICMP协议

ICMP（Internet Control Message Protocol）是网络层的辅助协议，用于在IP主机和路由器之间传递控制消息。

### ICMP报文类型

1. **差错报告报文**：
   - 目的不可达
   - 超时
   - 参数问题
   - 重定向

2. **查询报文**：
   - 回显请求/应答（ping）
   - 时间戳请求/应答
   - 地址掩码请求/应答

## ping命令

ping是最常用的网络诊断工具，基于ICMP协议。

### ping工作原理

1. 发送端发送ICMP回显请求报文
2. 接收端返回ICMP回显应答报文
3. 发送端计算往返时间（RTT）

### ping输出信息

- **目标IP地址**：被测试设备的IP
- **数据包大小**：发送的数据包字节数
- **响应时间**：往返时间（毫秒）
- **TTL**：生存时间，每经过一个路由器减1

### ping常用选项

- `-c count`：发送指定次数的请求
- `-i interval`：设置发送间隔
- `-s size`：设置数据包大小
- `-t`：持续ping（Windows）

## traceroute命令

traceroute用于追踪数据包从源到目标的路径。

### traceroute工作原理

1. 发送TTL=1的数据包
2. 第一个路由器返回ICMP超时消息
3. 递增TTL，重复上述过程
4. 直到到达目标主机

### 实际应用场景

1. **网络连通性测试**：判断两台主机是否可达
2. **网络延迟分析**：测量网络响应时间
3. **路径追踪**：定位网络故障点
4. **MTU发现**：确定最大传输单元

## 总结

ICMP是网络诊断的基础协议，ping和traceroute是网络管理员必备的工具。
''',

    'NAT与VPN.md': '''# NAT与VPN

## NAT概述

NAT（Network Address Translation）是一种将私有IP地址转换为公有IP地址的技术。

### NAT的作用

1. **节省IP地址**：多个内部设备共享一个公有IP
2. **增强安全性**：隐藏内部网络结构
3. **解决IP地址冲突**

### NAT类型

1. **静态NAT**：一对一映射，固定私有IP到公有IP
2. **动态NAT**：多对多映射，从地址池分配公有IP
3. **PAT（端口地址转换）**：一对多映射，通过端口区分不同连接

### PAT工作原理

1. 内部主机发送数据包到外部
2. NAT设备将源IP替换为公有IP，源端口替换为随机端口
3. NAT设备记录映射关系
4. 外部返回数据包时，NAT设备根据端口号转发到正确的内部主机

## VPN概述

VPN（Virtual Private Network）是一种通过公共网络建立安全私有连接的技术。

### VPN的作用

1. **远程访问**：允许远程用户安全访问企业网络
2. **站点间连接**：连接多个地理位置的网络
3. **数据加密**：保护数据传输安全

### VPN协议

1. **IPsec**：
   - 工作在网络层
   - 提供数据加密和认证
   - 支持隧道模式和传输模式

2. **SSL/TLS VPN**：
   - 工作在应用层
   - 通过浏览器访问
   - 无需安装客户端

3. **PPTP**：
   - 工作在数据链路层
   - 微软开发
   - 加密强度较低

4. **L2TP**：
   - 工作在数据链路层
   - 需要配合IPsec使用

### VPN工作流程

1. 建立安全隧道
2. 数据加密传输
3. 隧道内传输私有数据
4. 到达目的地后解密

## 总结

NAT解决了IP地址短缺问题，VPN提供了安全的远程访问能力。
''',

    'TCP可靠传输.md': '''# TCP可靠传输

## 可靠传输概述

TCP（Transmission Control Protocol）是一种面向连接的可靠传输协议，确保数据的正确传输。

### 可靠传输的实现机制

1. **确认机制**：接收方收到数据后发送确认
2. **重传机制**：发送方超时后重传未确认的数据
3. **序号机制**：为每个字节分配唯一序号
4. **流量控制**：防止发送方过快淹没接收方
5. **拥塞控制**：防止网络拥塞

## 序号与确认号

### 序号

序号用于标识数据在流中的位置，TCP为每个字节分配一个序号。

- **初始序号（ISN）**：连接建立时生成的随机序号
- **序号递增**：发送数据后，序号增加数据长度

### 确认号

确认号表示期望接收的下一个字节的序号。

- **累积确认**：确认号表示所有小于该序号的数据都已正确接收
- **选择性确认（SACK）**：可以确认非连续的段

## 重传机制

### 超时重传

发送方在发送数据后启动定时器，如果在超时时间内未收到确认，则重传数据。

**超时时间的计算**：
- RTO = max(1s, min(60s, β × SRTT))
- SRTT = α × SRTT + (1-α) × RTT

### 快速重传

当收到3个重复的ACK时，立即重传丢失的段，无需等待超时。

**工作原理**：
1. 发送方发送段1、2、3、4
2. 段2丢失
3. 接收方收到段3、4后，发送ACK 2（期望收到段2）
4. 发送方收到3个ACK 2后，立即重传段2

## 数据完整性

### 校验和

TCP在每个段中包含校验和，用于检测数据传输过程中的错误。

**计算方法**：
1. 将TCP段（包括伪头部）视为16位字的序列
2. 对所有字进行累加
3. 取反得到校验和

### 序列号检查

接收方检查序列号是否按顺序到达，检测重复或乱序的段。

## 总结

TCP通过多种机制实现可靠传输，是互联网中最重要的传输协议之一。
''',

    '慢启动.md': '''# TCP慢启动

## 慢启动概述

慢启动是TCP拥塞控制的初始阶段，用于探测网络的可用带宽。

### 慢启动原理

1. **初始阶段**：cwnd（拥塞窗口）初始值为1个MSS（最大报文段大小）
2. **指数增长**：每收到一个ACK，cwnd增加1个MSS
3. **快速探测**：在短时间内快速增加发送速率

### 慢启动过程

1. TCP连接建立后，cwnd = 1
2. 发送第一个段
3. 收到ACK后，cwnd = 2
4. 发送两个段
5. 收到两个ACK后，cwnd = 4
6. 继续指数增长，直到达到ssthresh（慢启动阈值）

## 慢启动阈值（ssthresh）

ssthresh是慢启动阶段和拥塞避免阶段的分界点。

### ssthresh的作用

- **cwnd < ssthresh**：使用慢启动，指数增长
- **cwnd >= ssthresh**：使用拥塞避免，线性增长

### ssthresh的调整

- **初始值**：通常设置为较大值（如65535字节）
- **拥塞发生时**：ssthresh = cwnd / 2

## 慢启动示例

假设MSS=1460字节，ssthresh=10：

| RTT | cwnd（段数） | 发送字节数 |
|-----|-------------|-----------|
| 1 | 1 | 1460 |
| 2 | 2 | 2920 |
| 3 | 4 | 5840 |
| 4 | 8 | 11680 |
| 5 | 10 | 14600（达到ssthresh） |

## 慢启动的优点

1. **快速适应**：能够快速探测网络带宽
2. **避免拥塞**：初始发送速率低，不会立即导致拥塞
3. **灵活性**：根据网络情况动态调整

## 慢启动的缺点

1. **启动延迟**：在带宽较大的网络中，达到满速率需要多个RTT
2. **对丢包敏感**：任何丢包都会导致cwnd重置为1

## 总结

慢启动是TCP拥塞控制的重要组成部分，通过指数增长策略快速探测网络容量。
''',

    '拥塞避免.md': '''# TCP拥塞避免

## 拥塞避免概述

拥塞避免是TCP拥塞控制的第二阶段，当cwnd超过ssthresh后进入。

### 拥塞避免原理

1. **线性增长**：每经过一个RTT，cwnd增加1个MSS
2. **缓慢增加**：相比慢启动，增长速度大幅降低
3. **探测拥塞**：通过缓慢增加发送速率，寻找网络的拥塞临界点

### 拥塞避免过程

1. cwnd达到ssthresh后，进入拥塞避免阶段
2. 每收到一个完整RTT的ACK，cwnd增加1
3. 持续线性增长，直到检测到拥塞

## 拥塞检测

### 超时检测

当发送方的定时器超时，认为发生了拥塞。

**处理方式**：
- ssthresh = cwnd / 2
- cwnd = 1
- 重新进入慢启动阶段

### 重复ACK检测

当收到3个重复的ACK，认为发生了拥塞（快速重传）。

**处理方式**：
- ssthresh = cwnd / 2
- cwnd = ssthresh
- 进入快速恢复阶段（如果支持）

## 拥塞避免示例

假设ssthresh=10，cwnd从10开始：

| RTT | cwnd（段数） | 说明 |
|-----|-------------|------|
| 1 | 10 | 进入拥塞避免 |
| 2 | 11 | 线性增长 |
| 3 | 12 | 线性增长 |
| 4 | 13 | 线性增长 |
| 5 | 14 | 检测到拥塞 |
| 6 | 7 | ssthresh=7, cwnd=1（超时）或cwnd=7（快速重传） |

## 加法增大/乘法减小（AIMD）

拥塞避免采用AIMD策略：

- **加法增大**：拥塞避免阶段，cwnd线性增加
- **乘法减小**：检测到拥塞时，cwnd减半

**优点**：
- 稳定性好：收敛到公平的带宽分配
- 响应迅速：快速降低发送速率避免拥塞

## 总结

拥塞避免通过线性增长策略在提高吞吐量和避免拥塞之间取得平衡。
''',

    '快速重传.md': '''# TCP快速重传

## 快速重传概述

快速重传是TCP拥塞控制的一种优化机制，用于在检测到丢包时立即重传，无需等待超时。

### 快速重传原理

1. **重复ACK检测**：当接收方收到乱序的段时，发送重复的ACK
2. **3个重复ACK触发**：发送方收到3个重复的ACK后，认为对应的数据段丢失
3. **立即重传**：发送方立即重传丢失的段，无需等待定时器超时

### 快速重传过程

1. 发送方发送段1、2、3、4、5
2. 段3丢失
3. 接收方收到段4，发送ACK 3（期望收到段3）
4. 接收方收到段5，发送ACK 3
5. 发送方收到第3个ACK 3
6. 发送方立即重传段3
7. 接收方收到段3，发送ACK 6（确认所有段）

## 快速重传与超时重传的对比

| 特性 | 超时重传 | 快速重传 |
|------|---------|---------|
| 触发条件 | 定时器超时 | 3个重复ACK |
| 响应时间 | 较长（RTT量级） | 较短（即时） |
| cwnd处理 | 重置为1 | 设置为ssthresh |
| 后续阶段 | 慢启动 | 快速恢复 |

## 选择性确认（SACK）

SACK是对快速重传的增强，允许接收方确认非连续的段。

### SACK工作原理

1. 接收方在ACK中包含SACK选项
2. SACK选项列出已收到但不连续的段范围
3. 发送方根据SACK信息确定需要重传的段

### SACK示例

假设发送了段1-5，段3丢失：

1. 接收方收到段1、2、4、5
2. ACK中包含SACK: [4-5]
3. 发送方知道段3丢失，立即重传

## 快速重传的优点

1. **减少延迟**：避免了超时等待时间
2. **提高吞吐量**：更快地恢复丢失的数据
3. **更好的网络利用率**：减少了不必要的带宽浪费

## 总结

快速重传是TCP拥塞控制的重要优化，通过检测重复ACK快速响应丢包。
''',

    '快速恢复.md': '''# TCP快速恢复

## 快速恢复概述

快速恢复是TCP拥塞控制的第四个阶段，与快速重传配合使用，用于在检测到丢包后快速恢复发送速率。

### 快速恢复原理

1. **不完全退回到慢启动**：检测到丢包时，不将cwnd重置为1
2. **快速恢复速率**：将cwnd设置为ssthresh，然后线性增长
3. **维持较高发送速率**：避免因单个丢包导致发送速率大幅下降

### 快速恢复过程

1. 检测到3个重复ACK（快速重传触发）
2. ssthresh = cwnd / 2
3. cwnd = ssthresh + 3（考虑已收到的3个重复ACK）
4. 重传丢失的段
5. 每收到一个ACK，cwnd增加1
6. 当cwnd恢复到原来的值时，进入拥塞避免阶段

## 快速恢复示例

假设cwnd=12，ssthresh=10：

1. 收到3个重复ACK
2. ssthresh = 12 / 2 = 6
3. cwnd = 6 + 3 = 9
4. 重传丢失的段
5. 收到ACK后，cwnd = 10
6. 继续线性增长，进入拥塞避免

## 快速恢复与慢启动的对比

| 场景 | 慢启动 | 快速恢复 |
|------|--------|---------|
| 丢包后cwnd | 1 | ssthresh + 3 |
| 恢复速度 | 慢（指数增长） | 快（线性增长） |
| 适用情况 | 严重拥塞 | 轻微丢包 |

## 快速恢复的优点

1. **快速恢复**：避免退回到慢启动，更快恢复发送速率
2. **减少抖动**：减少因丢包导致的吞吐量波动
3. **更好的用户体验**：减少网络延迟和卡顿

## TCP Reno与TCP Tahoe

- **TCP Tahoe**：只支持慢启动和拥塞避免，不支持快速重传和快速恢复
- **TCP Reno**：支持快速重传和快速恢复，是目前主流的TCP实现

## 总结

快速恢复是TCP拥塞控制的重要优化，通过避免完全退回到慢启动，提高了网络吞吐量和稳定性。
''',

    'DNS域名系统.md': '''# DNS域名系统

## DNS概述

DNS（Domain Name System）是互联网中用于将域名转换为IP地址的分布式系统。

### DNS的作用

1. **域名解析**：将www.example.com转换为IP地址
2. **负载均衡**：将同一域名解析到不同服务器
3. **邮件路由**：解析MX记录，确定邮件服务器

### DNS层次结构

DNS采用树形层次结构：

- **根域名服务器**：13台根服务器，管理顶级域名
- **顶级域名服务器**：管理.com、.cn等顶级域名
- **权威域名服务器**：管理特定域名的解析
- **本地域名服务器**：用户本地的DNS服务器

## DNS解析过程

### 递归查询

客户端向本地DNS服务器发送请求，本地服务器负责完成整个解析过程。

1. 客户端查询www.example.com
2. 本地服务器查询根服务器
3. 根服务器返回.com服务器地址
4. 本地服务器查询.com服务器
5. .com服务器返回example.com服务器地址
6. 本地服务器查询example.com服务器
7. example.com服务器返回IP地址
8. 本地服务器返回IP地址给客户端

### 迭代查询

服务器只返回下一个服务器的地址，客户端负责继续查询。

### DNS缓存

为了提高效率，DNS使用缓存机制：

- **客户端缓存**：操作系统或浏览器缓存
- **本地服务器缓存**：本地DNS服务器缓存
- **TTL（生存时间）**：缓存的有效期

## DNS记录类型

常见的DNS记录类型：

| 类型 | 名称 | 用途 |
|------|------|------|
| A | 地址记录 | 域名到IPv4地址 |
| AAAA | IPv6地址记录 | 域名到IPv6地址 |
| CNAME | 别名记录 | 域名到另一个域名 |
| MX | 邮件交换记录 | 域名的邮件服务器 |
| NS | 名称服务器记录 | 域名的权威服务器 |
| TXT | 文本记录 | 存储任意文本信息 |
| SRV | 服务记录 | 指定服务的位置 |

## DNS安全

### DNS劫持

攻击者篡改DNS解析结果，将用户导向恶意网站。

### DNSSEC

DNS安全扩展，通过数字签名验证DNS记录的真实性。

## 总结

DNS是互联网的基础设施，使得用户能够使用易于记忆的域名访问网站。
''',

    'HTTP与HTTPS.md': '''# HTTP与HTTPS

## HTTP概述

HTTP（Hypertext Transfer Protocol）是用于在Web上传输超文本的协议。

### HTTP特点

- **无状态**：服务器不保存客户端的状态信息
- **明文传输**：数据以明文形式传输，不安全
- **基于TCP**：建立在TCP连接之上

### HTTP请求方法

| 方法 | 用途 |
|------|------|
| GET | 获取资源 |
| POST | 提交数据 |
| PUT | 更新资源 |
| DELETE | 删除资源 |
| HEAD | 获取资源头部 |
| OPTIONS | 获取服务器支持的方法 |

### HTTP状态码

| 状态码 | 类别 | 说明 |
|--------|------|------|
| 1xx | 信息性 | 请求已接收，继续处理 |
| 2xx | 成功 | 请求成功处理 |
| 3xx | 重定向 | 需要进一步操作 |
| 4xx | 客户端错误 | 请求有错误 |
| 5xx | 服务器错误 | 服务器处理失败 |

## HTTPS概述

HTTPS（HTTP Secure）是HTTP的安全版本，通过SSL/TLS加密传输数据。

### HTTPS特点

- **加密传输**：数据使用对称加密传输
- **身份认证**：通过数字证书验证服务器身份
- **数据完整性**：防止数据篡改

### HTTPS工作流程

1. **建立TCP连接**：客户端与服务器建立TCP连接
2. **SSL/TLS握手**：
   - 客户端发送支持的加密套件
   - 服务器发送数字证书
   - 客户端验证证书
   - 协商对称密钥
3. **加密传输**：使用对称密钥加密数据传输
4. **关闭连接**：关闭SSL/TLS连接和TCP连接

### HTTPS与HTTP的对比

| 特性 | HTTP | HTTPS |
|------|------|-------|
| 端口 | 80 | 443 |
| 安全性 | 明文传输 | 加密传输 |
| 速度 | 较快 | 较慢（加密开销） |
| 证书 | 不需要 | 需要CA证书 |

## HTTP/2

HTTP/2是HTTP的新版本，提供更好的性能：

- **多路复用**：一个TCP连接上同时传输多个请求/响应
- **头部压缩**：减少头部传输大小
- **服务器推送**：服务器主动推送资源

## 总结

HTTPS通过加密和认证提供了安全的Web通信，是现代网站的标配。
''',

    'DHCP.md': '''# DHCP

## DHCP概述

DHCP（Dynamic Host Configuration Protocol）是一种自动分配IP地址的协议。

### DHCP的作用

1. **自动分配IP地址**：无需手动配置
2. **统一管理**：集中管理IP地址分配
3. **减少错误**：避免IP地址冲突

### DHCP工作过程（四步握手）

1. **DHCP Discover**：客户端广播发送发现请求
2. **DHCP Offer**：服务器发送IP地址提议
3. **DHCP Request**：客户端请求使用该IP地址
4. **DHCP Acknowledge**：服务器确认分配

### DHCP消息类型

| 类型 | 用途 |
|------|------|
| Discover | 发现可用的DHCP服务器 |
| Offer | 提供IP地址 |
| Request | 请求IP地址 |
| Acknowledge | 确认分配 |
| Release | 释放IP地址 |
| Decline | 拒绝IP地址 |

## DHCP分配方式

1. **自动分配**：永久分配一个IP地址
2. **动态分配**：在租约期内分配IP地址，到期后收回
3. **手动分配**：管理员指定IP地址，由DHCP服务器分配

### DHCP租约

- **租约期限**：IP地址的使用期限
- **续租**：客户端在租约到期前请求续租
- **释放**：客户端主动释放IP地址

## DHCP选项

DHCP可以分配除IP地址外的其他配置信息：

| 选项 | 用途 |
|------|------|
| 子网掩码 | 网络掩码 |
| 默认网关 | 默认路由 |
| DNS服务器 | DNS地址 |
| 域名 | 主机域名 |
| MTU | 最大传输单元 |

## DHCP中继

当DHCP客户端和服务器不在同一网段时，需要DHCP中继代理。

### DHCP中继工作原理

1. 客户端发送DHCP Discover（广播）
2. 中继代理接收并转发给DHCP服务器（单播）
3. 服务器发送DHCP Offer给中继代理
4. 中继代理转发给客户端（广播）

## 总结

DHCP简化了网络配置管理，是现代网络中不可或缺的协议。
''',

    'FTP与邮件协议.md': '''# FTP与邮件协议

## FTP概述

FTP（File Transfer Protocol）是用于在网络上传输文件的协议。

### FTP特点

- **基于TCP**：使用两个TCP连接（控制连接和数据连接）
- **明文传输**：用户名和密码以明文传输
- **主动/被动模式**：支持两种数据连接模式

### FTP连接类型

1. **控制连接**：用于传输命令，端口21
2. **数据连接**：用于传输文件数据

### FTP主动模式

1. 客户端打开随机端口，发送PORT命令
2. 服务器从端口20主动连接客户端的指定端口

### FTP被动模式

1. 客户端发送PASV命令
2. 服务器打开随机端口，告知客户端
3. 客户端连接服务器的指定端口

### SFTP

SFTP（SSH File Transfer Protocol）是安全的FTP版本，通过SSH加密传输。

## 邮件协议概述

电子邮件系统使用多种协议协同工作。

### SMTP

SMTP（Simple Mail Transfer Protocol）用于发送邮件。

- 端口：25（明文）或465（SSL）
- 工作方式：客户端→服务器→服务器→接收方

### POP3

POP3（Post Office Protocol）用于接收邮件。

- 端口：110（明文）或995（SSL）
- 工作方式：客户端下载邮件后，服务器默认删除邮件

### IMAP

IMAP（Internet Message Access Protocol）用于接收邮件。

- 端口：143（明文）或993（SSL）
- 工作方式：邮件保留在服务器上，客户端同步操作

### POP3与IMAP对比

| 特性 | POP3 | IMAP |
|------|------|------|
| 邮件存储 | 下载到本地 | 保留在服务器 |
| 多设备同步 | 不支持 | 支持 |
| 离线访问 | 支持 | 部分支持 |
| 服务器负载 | 低 | 高 |

## 邮件传输流程

1. 发件人使用MUA（邮件用户代理）编写邮件
2. MUA使用SMTP发送邮件到发件人服务器
3. 发件人服务器使用SMTP转发到收件人服务器
4. 收件人使用MUA通过POP3或IMAP接收邮件

## 总结

FTP用于文件传输，邮件协议用于电子邮件系统，都是互联网中重要的应用层协议。
''',

    'Socket编程.md': '''# Socket编程

## Socket概述

Socket（套接字）是网络通信的编程接口，用于在不同主机之间传输数据。

### Socket类型

1. **流式套接字（SOCK_STREAM）**：基于TCP，提供可靠的字节流传输
2. **数据报套接字（SOCK_DGRAM）**：基于UDP，提供不可靠的数据包传输
3. **原始套接字（SOCK_RAW）**：直接访问网络层，用于特殊用途

## TCP Socket编程

### 服务器端流程

1. **创建Socket**：socket()
2. **绑定地址**：bind()
3. **监听连接**：listen()
4. **接受连接**：accept()
5. **数据传输**：recv()/send()
6. **关闭连接**：close()

### 客户端流程

1. **创建Socket**：socket()
2. **连接服务器**：connect()
3. **数据传输**：send()/recv()
4. **关闭连接**：close()

### TCP Socket示例（Python）

```python
# 服务器端
import socket

server = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
server.bind(('localhost', 8080))
server.listen(5)

client, addr = server.accept()
data = client.recv(1024)
print(f"收到: {data.decode()}")
client.send(b"Hello from server")
client.close()
server.close()
```

```python
# 客户端
import socket

client = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
client.connect(('localhost', 8080))
client.send(b"Hello from client")
data = client.recv(1024)
print(f"收到: {data.decode()}")
client.close()
```

## UDP Socket编程

### UDP特点

- 无连接，无需建立连接
- 不可靠，不保证数据到达
- 速度快，开销小

### UDP编程流程

1. **创建Socket**：socket()
2. **绑定地址**（可选）：bind()
3. **数据传输**：sendto()/recvfrom()
4. **关闭Socket**：close()

### UDP Socket示例（Python）

```python
# 服务器端
import socket

server = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
server.bind(('localhost', 8080))

data, addr = server.recvfrom(1024)
print(f"收到来自{addr}的消息: {data.decode()}")
server.sendto(b"Hello from server", addr)
server.close()
```

```python
# 客户端
import socket

client = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
client.sendto(b"Hello from client", ('localhost', 8080))
data, addr = client.recvfrom(1024)
print(f"收到来自{addr}的消息: {data.decode()}")
client.close()
```

## Socket选项

常用的Socket选项：

- **SO_REUSEADDR**：允许重用地址
- **SO_TIMEOUT**：设置超时时间
- **SO_SNDBUF/SO_RCVBUF**：设置发送/接收缓冲区大小

## 总结

Socket编程是网络编程的基础，掌握TCP和UDP的Socket编程是开发网络应用的必备技能。
''',

    '加密算法.md': '''# 加密算法

## 加密概述

加密是将明文转换为密文的过程，用于保护数据的机密性。

### 加密算法分类

1. **对称加密**：加密和解密使用相同的密钥
2. **非对称加密**：加密和解密使用不同的密钥
3. **哈希函数**：将任意长度数据转换为固定长度摘要

## 对称加密

### 对称加密特点

- **速度快**：加密解密效率高
- **密钥管理困难**：需要安全地分发和管理密钥
- **适合大量数据**：常用于加密数据传输

### 常见对称加密算法

1. **DES**：
   - 56位密钥
   - 已被破解，不再安全
   
2. **AES**：
   - 128/192/256位密钥
   - 目前最安全的对称加密算法
   - 广泛应用于各种场景

3. **3DES**：
   - 三重DES加密
   - 比DES更安全，但速度较慢

### 对称加密模式

1. **ECB（电子密码本）**：相同明文产生相同密文，不安全
2. **CBC（密码分组链接）**：使用IV，安全性较高
3. **CFB（密码反馈）**：流式加密，适合实时通信
4. **OFB（输出反馈）**：流式加密，与CFB类似

## 非对称加密

### 非对称加密特点

- **安全性高**：私钥不需要传输
- **速度慢**：加密解密效率低
- **适合密钥交换**：常用于交换对称密钥

### 常见非对称加密算法

1. **RSA**：
   - 基于大数分解难题
   - 广泛应用于数字签名和密钥交换
   - 密钥长度通常为2048或4096位

2. **ECC（椭圆曲线加密）**：
   - 基于椭圆曲线数学难题
   - 相同安全级别下，密钥更短
   - 适合移动设备和物联网

3. **DSA**：
   - 专门用于数字签名
   - 基于离散对数难题

### 非对称加密工作流程

1. 生成公钥和私钥对
2. 公钥公开，私钥保密
3. 使用公钥加密，私钥解密
4. 使用私钥签名，公钥验证

## 哈希函数

### 哈希函数特点

- **单向性**：无法从哈希值反推原始数据
- **唯一性**：不同输入产生不同哈希值（理想情况）
- **固定长度**：无论输入多长，输出长度固定

### 常见哈希函数

1. **MD5**：
   - 128位输出
   - 已被破解，不再安全

2. **SHA-1**：
   - 160位输出
   - 已被破解，不再安全

3. **SHA-2**：
   - SHA-256（256位）、SHA-512（512位）
   - 目前安全，广泛应用

4. **SHA-3**：
   - 新一代哈希函数
   - 抗量子攻击

## 总结

对称加密用于数据加密，非对称加密用于密钥交换和签名，哈希函数用于数据完整性验证。
''',

    '数字签名与证书.md': '''# 数字签名与证书

## 数字签名概述

数字签名是一种用于验证数据真实性和完整性的技术。

### 数字签名特点

- **不可否认性**：签名者无法否认自己的签名
- **完整性**：数据被篡改后签名无效
- **真实性**：可以验证签名者的身份

### 数字签名工作流程

1. **哈希运算**：对原始数据进行哈希计算
2. **私钥加密**：使用签名者的私钥加密哈希值
3. **发送数据**：发送原始数据和签名
4. **公钥解密**：接收方使用签名者的公钥解密签名
5. **验证哈希**：重新计算哈希值并与解密结果比较

## 数字证书概述

数字证书是由CA（证书颁发机构）颁发的，用于证明公钥所有者的身份。

### 数字证书内容

- **版本号**：证书格式版本
- **序列号**：证书的唯一标识
- **签名算法**：CA使用的签名算法
- **颁发者**：CA的名称
- **有效期**：证书的有效期限
- **主体**：证书所有者的信息
- **主体公钥**：证书所有者的公钥
- **CA签名**：CA对证书的签名

### 数字证书类型

1. **SSL/TLS证书**：用于网站安全
2. **代码签名证书**：用于软件代码签名
3. **电子邮件证书**：用于电子邮件加密和签名

### 证书信任链

1. **根证书**：CA的自签名证书
2. **中间证书**：由根CA颁发的CA证书
3. **终端证书**：颁发给用户或服务器的证书

验证过程：从终端证书向上验证，直到根证书。

## CA（证书颁发机构）

### CA的作用

1. **验证身份**：验证证书申请者的身份
2. **颁发证书**：颁发包含公钥和身份信息的证书
3. **吊销证书**：在证书过期或密钥泄露时吊销证书

### 知名CA

- **Let's Encrypt**：免费SSL证书
- **DigiCert**：全球最大的CA
- **GlobalSign**：知名CA
- **Verizon**：企业级CA

## 证书吊销

### CRL（证书吊销列表）

CA定期发布吊销的证书列表，客户端需要下载并检查。

### OCSP（在线证书状态协议）

客户端实时查询证书的状态，比CRL更及时。

## 总结

数字签名确保数据的真实性和完整性，数字证书确保公钥的合法性，是网络安全的基础设施。
''',

    'SSL-TLS.md': '''# SSL/TLS

## SSL/TLS概述

SSL（Secure Sockets Layer）和TLS（Transport Layer Security）是用于在网络上提供安全通信的协议。

### SSL/TLS的作用

1. **加密传输**：使用对称加密保护数据传输
2. **身份认证**：通过数字证书验证服务器（和客户端）身份
3. **数据完整性**：使用MAC防止数据篡改

### SSL与TLS的关系

- SSL是早期版本（SSL 1.0/2.0/3.0）
- TLS是SSL的升级版本（TLS 1.0/1.1/1.2/1.3）
- TLS 1.0 = SSL 3.1，TLS向后兼容SSL

## SSL/TLS握手过程

### TLS 1.2握手

1. **ClientHello**：客户端发送支持的加密套件和随机数
2. **ServerHello**：服务器选择加密套件和发送随机数
3. **Certificate**：服务器发送数字证书
4. **ServerKeyExchange**：服务器发送密钥交换信息
5. **ServerHelloDone**：服务器完成问候
6. **ClientKeyExchange**：客户端发送密钥交换信息
7. **ChangeCipherSpec**：客户端通知开始使用新密钥
8. **Finished**：客户端发送握手完成消息
9. **ChangeCipherSpec**：服务器通知开始使用新密钥
10. **Finished**：服务器发送握手完成消息

### TLS 1.3优化

TLS 1.3简化了握手过程，只需1-RTT：

- 删除了不安全的加密套件
- 合并了多个消息
- 支持0-RTT（恢复连接时）

## SSL/TLS加密套件

加密套件包含：

1. **密钥交换算法**：RSA、DH、ECDH
2. **对称加密算法**：AES、ChaCha20
3. **哈希算法**：SHA-256、SHA-384

### 常见加密套件

- **TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384**
- **TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384**
- **TLS_CHACHA20_POLY1305_SHA256**

## SSL/TLS应用

### HTTPS

HTTPS = HTTP + SSL/TLS，用于安全的Web通信。

### FTPS

FTPS = FTP + SSL/TLS，用于安全的文件传输。

### IMAPS

IMAPS = IMAP + SSL/TLS，用于安全的邮件接收。

### VPN

IPsec VPN和SSL VPN都使用SSL/TLS进行加密。

## SSL/TLS漏洞

### 历史漏洞

- **Heartbleed**：OpenSSL漏洞，泄露内存数据
- **POODLE**：SSL 3.0漏洞，降级攻击
- **BEAST**：TLS 1.0漏洞，CBC模式攻击

### 防护措施

- 使用最新版本的TLS（TLS 1.2或1.3）
- 禁用SSL和旧版TLS
- 使用强加密套件

## 总结

SSL/TLS是网络安全的基础，通过加密、认证和完整性保护提供安全的通信。
''',

    '防火墙与IDS.md': '''# 防火墙与IDS

## 防火墙概述

防火墙是一种网络安全设备，用于控制进出网络的流量。

### 防火墙的作用

1. **访问控制**：允许或拒绝特定流量
2. **网络隔离**：隔离内部网络和外部网络
3. **日志记录**：记录网络活动
4. **NAT**：网络地址转换

### 防火墙类型

1. **包过滤防火墙**：
   - 工作在网络层
   - 根据IP地址、端口、协议过滤
   - 速度快，功能简单

2. **应用层防火墙**：
   - 工作在应用层
   - 可以检查应用层数据
   - 功能强大，速度较慢

3. **状态检测防火墙**：
   - 跟踪连接状态
   - 只允许响应内部发起的连接
   - 安全性较高

### 防火墙规则

防火墙规则通常包含：

- **源IP地址**
- **目标IP地址**
- **源端口**
- **目标端口**
- **协议**（TCP/UDP/ICMP）
- **动作**（允许/拒绝）

### 防火墙部署位置

1. **网络边界**：连接内部网络和互联网
2. **内部网络**：隔离不同部门
3. **服务器前面**：保护重要服务器

## IDS概述

IDS（Intrusion Detection System）是一种检测网络入侵行为的系统。

### IDS的作用

1. **异常检测**：检测异常的网络行为
2. **攻击识别**：识别已知的攻击模式
3. **实时告警**：及时通知管理员
4. **日志记录**：记录入侵事件

### IDS类型

1. **NIDS（网络入侵检测系统）**：
   - 监控网络流量
   - 检测网络层和传输层的攻击
   - 不影响网络性能

2. **HIDS（主机入侵检测系统）**：
   - 安装在主机上
   - 检测主机层面的攻击
   - 可以检测文件篡改

### IDS检测方法

1. **特征检测**：
   - 基于已知攻击模式
   - 准确率高，漏报率低
   - 无法检测未知攻击

2. **异常检测**：
   - 基于正常行为模型
   - 可以检测未知攻击
   - 误报率较高

## IPS概述

IPS（Intrusion Prevention System）是IDS的升级版，可以主动阻止攻击。

### IPS与IDS的区别

| 特性 | IDS | IPS |
|------|-----|-----|
| 响应方式 | 被动检测，告警 | 主动阻止攻击 |
| 部署方式 | 旁路部署 | 在线部署 |
| 对网络影响 | 无 | 可能影响性能 |
| 安全性 | 检测 | 检测+阻止 |

## 总结

防火墙用于防止未授权访问，IDS用于检测入侵行为，IPS用于主动阻止攻击，三者共同构成网络安全防线。
'''
}

for filename, content in docs_to_generate.items():
    file_path = documents_dir / filename
    if not file_path.exists():
        file_path.write_text(content, encoding='utf-8')
        print(f'已创建: {filename}')
    else:
        print(f'已存在: {filename}')

print(f'\n共处理 {len(docs_to_generate)} 个文档')
