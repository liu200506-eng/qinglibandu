import os
from pathlib import Path

documents_dir = Path(__file__).parent / 'knowledge_base' / 'computer_network' / 'documents'

empty_docs_content = {
    'ARP协议.md': '''# ARP协议

## ARP概述

ARP（Address Resolution Protocol）是地址解析协议，用于将IP地址转换为MAC地址。

### ARP的作用

在以太网中，数据帧需要使用MAC地址进行传输，但应用层使用的是IP地址。ARP协议解决了IP地址到MAC地址的映射问题。

## ARP工作过程

### ARP请求

1. 主机A需要向主机B发送数据，但只知道主机B的IP地址
2. 主机A广播ARP请求报文，包含自己的IP地址和MAC地址，以及目标IP地址
3. 局域网内所有主机都收到ARP请求

### ARP响应

1. 主机B收到ARP请求，发现目标IP地址是自己的
2. 主机B发送ARP响应报文，包含自己的IP地址和MAC地址
3. 主机A收到ARP响应，将主机B的IP地址和MAC地址存入ARP缓存

### ARP缓存

ARP缓存是主机中存储IP地址与MAC地址映射关系的临时表。

- **动态条目**：通过ARP协议自动获取，有过期时间
- **静态条目**：手动配置，永久有效

## ARP类型

### 普通ARP

用于IPv4地址到MAC地址的解析。

### RARP

RARP（Reverse ARP）用于将MAC地址转换为IP地址，已被DHCP取代。

### Proxy ARP

代理ARP是一种技术，当主机询问同一网段但实际在不同网段的IP地址时，网关可以代理回复。

## ARP攻击

### ARP欺骗

攻击者发送伪造的ARP响应，篡改受害者的ARP缓存。

**攻击后果**：
- 中间人攻击：截获双方通信
- 拒绝服务：使目标无法通信

### ARP防护

1. **静态ARP绑定**：手动配置IP地址与MAC地址的映射
2. **ARP检测工具**：检测异常ARP流量
3. **交换机防护**：部分交换机支持ARP防护功能

## 总结

ARP协议是以太网通信的基础，理解其工作原理对于网络管理和安全至关重要。
''',

    '以太网与MAC地址.md': '''# 以太网与MAC地址

## 以太网概述

以太网是目前最广泛使用的局域网技术，由施乐公司在1973年发明。

### 以太网特点

- **共享介质**：早期使用总线型拓扑，多个设备共享同一传输介质
- **CSMA/CD**：载波监听多路访问/冲突检测
- **帧格式**：定义了数据帧的结构

### CSMA/CD工作原理

1. **监听**：发送前监听介质是否空闲
2. **发送**：如果空闲，发送数据
3. **检测**：发送过程中检测是否发生冲突
4. **退避**：如果检测到冲突，停止发送，等待随机时间后重发

## MAC地址

### MAC地址概述

MAC（Media Access Control）地址是网络接口的物理地址，用于在局域网中标识设备。

### MAC地址结构

MAC地址由48位二进制数组成，通常表示为十六进制形式，如00-1A-2B-3C-4D-5E。

- **前24位**：厂商标识符（OUI），由IEEE分配
- **后24位**：设备标识符，由厂商分配

### MAC地址类型

1. **单播地址**：用于一对一通信
2. **广播地址**：FF-FF-FF-FF-FF-FF，用于一对所有通信
3. **组播地址**：用于一对多通信

## 以太网帧格式

### IEEE 802.3帧格式

1. **前导码**：7字节，用于同步
2. **帧起始定界符**：1字节，标识帧的开始
3. **目的MAC地址**：6字节
4. **源MAC地址**：6字节
5. **长度/类型**：2字节
6. **数据**：46-1500字节
7. **FCS**：4字节，帧校验序列

### 最大传输单元（MTU）

以太网的MTU为1500字节，超过此长度的数据需要分片。

## 以太网发展

### 以太网标准

| 标准 | 速率 | 介质 |
|------|------|------|
| 10BASE-T | 10Mbps | 双绞线 |
| 100BASE-TX | 100Mbps | 双绞线 |
| 1000BASE-T | 1Gbps | 双绞线 |
| 10GBASE-T | 10Gbps | 双绞线 |

### 全双工模式

现代以太网支持全双工模式，可以同时发送和接收数据，消除了CSMA/CD的冲突问题。

## 总结

以太网是局域网的基础技术，MAC地址是设备在局域网中的唯一标识。
''',

    'TCP三次握手.md': '''# TCP三次握手

## TCP连接建立概述

TCP是面向连接的协议，在传输数据之前需要建立连接。三次握手是TCP连接建立的过程。

### 三次握手的目的

1. **确认双方的发送能力**：确保双方都能发送数据
2. **确认双方的接收能力**：确保双方都能接收数据
3. **同步序列号**：交换初始序列号（ISN）

## 三次握手过程

### 第一次握手（SYN）

1. 客户端发送SYN报文
2. 报文包含：
   - SYN标志位：1
   - 客户端初始序列号（ISN_c）
   - 其他选项

### 第二次握手（SYN+ACK）

1. 服务器收到SYN报文
2. 服务器发送SYN+ACK报文
3. 报文包含：
   - SYN标志位：1
   - ACK标志位：1
   - 服务器初始序列号（ISN_s）
   - 确认号：ISN_c + 1

### 第三次握手（ACK）

1. 客户端收到SYN+ACK报文
2. 客户端发送ACK报文
3. 报文包含：
   - ACK标志位：1
   - 确认号：ISN_s + 1

## 三次握手的必要性

### 为什么需要三次握手

1. **防止已失效的连接请求**：如果客户端发送的连接请求在网络中滞留，服务器收到后会发送SYN+ACK，客户端可以通过第三次握手判断是否需要这个连接
2. **同步序列号**：双方需要交换初始序列号，用于后续的数据传输
3. **确认双方能力**：确保双方都能发送和接收数据

### 能否用两次握手

两次握手不足以确认双方的接收能力。如果只进行两次握手，服务器无法确认客户端是否收到了SYN+ACK报文。

## TCP连接状态

### 客户端状态变化

- **CLOSED**：初始状态
- **SYN_SENT**：发送SYN后
- **ESTABLISHED**：收到SYN+ACK并发送ACK后

### 服务器状态变化

- **CLOSED**：初始状态
- **LISTEN**：监听状态
- **SYN_RCVD**：收到SYN后
- **ESTABLISHED**：收到ACK后

## 总结

三次握手是TCP连接建立的必要过程，通过三次交互确保双方的通信能力和序列号同步。
''',

    'TCP四次挥手.md': '''# TCP四次挥手

## TCP连接释放概述

TCP连接释放需要四次挥手，因为TCP是全双工协议，双方需要分别释放各自的连接。

### 四次挥手的目的

1. **通知对方不再发送数据**
2. **等待对方发送完剩余数据**
3. **确认双方都已完成数据传输**

## 四次挥手过程

### 第一次挥手（FIN）

1. 客户端发送FIN报文
2. 报文包含：
   - FIN标志位：1
   - 序列号：客户端当前序列号

### 第二次挥手（ACK）

1. 服务器收到FIN报文
2. 服务器发送ACK报文
3. 报文包含：
   - ACK标志位：1
   - 确认号：客户端序列号 + 1

此时，客户端到服务器的连接已经关闭，但服务器到客户端的连接仍然可以传输数据。

### 第三次挥手（FIN）

1. 服务器发送完所有数据后，发送FIN报文
2. 报文包含：
   - FIN标志位：1
   - 序列号：服务器当前序列号

### 第四次挥手（ACK）

1. 客户端收到FIN报文
2. 客户端发送ACK报文
3. 报文包含：
   - ACK标志位：1
   - 确认号：服务器序列号 + 1

## TIME_WAIT状态

### TIME_WAIT的作用

客户端发送第四次挥手后，进入TIME_WAIT状态，等待2MSL（最长报文段寿命）时间。

### TIME_WAIT的必要性

1. **确保最后一个ACK到达服务器**：如果ACK丢失，服务器会重传FIN，客户端需要在TIME_WAIT期间接收并重传ACK
2. **防止已失效的连接请求**：等待足够长的时间，确保网络中所有旧的报文段都已消失

### TIME_WAIT的问题

- **端口占用**：客户端在TIME_WAIT期间无法使用相同的端口
- **资源浪费**：长时间占用系统资源

## 半关闭状态

### 半关闭的概念

TCP支持半关闭，即一方可以关闭发送通道，但仍然可以接收数据。

### 半关闭的应用

- **HTTP连接**：客户端发送完请求后关闭发送通道，等待服务器响应
- **文件传输**：发送方发送完文件后关闭发送通道，等待接收方确认

## 总结

四次挥手是TCP连接释放的必要过程，通过四次交互确保双方都能正确释放连接。
''',

    '流量控制.md': '''# TCP流量控制

## 流量控制概述

流量控制是TCP的重要功能，用于防止发送方过快地发送数据，导致接收方缓冲区溢出。

### 流量控制的目的

1. **防止接收方缓冲区溢出**
2. **根据接收方的处理能力调整发送速率**
3. **保证数据的可靠传输**

## 滑动窗口机制

### 滑动窗口概述

TCP使用滑动窗口机制实现流量控制，接收方通过通告窗口（rwnd）告诉发送方自己的缓冲区可用空间。

### 窗口大小

窗口大小表示发送方可以连续发送的字节数，无需等待确认。

- **发送窗口**：由发送方维护，取决于接收方的通告窗口
- **接收窗口**：由接收方维护，取决于缓冲区可用空间

### 窗口滑动

1. 发送方发送数据
2. 接收方确认数据，并更新通告窗口
3. 发送方根据新的通告窗口调整发送窗口
4. 发送方继续发送数据

## 流量控制过程

### 初始阶段

1. 建立连接时，双方交换初始窗口大小
2. 发送方根据接收方的通告窗口发送数据

### 数据传输阶段

1. 接收方接收数据后，计算剩余缓冲区空间
2. 接收方在ACK报文中携带新的通告窗口
3. 发送方根据通告窗口调整发送速率

### 缓冲区满的情况

1. 如果接收方缓冲区已满，通告窗口为0
2. 发送方停止发送数据
3. 接收方处理完数据后，发送非零的通告窗口

## 流量控制与拥塞控制的区别

| 特性 | 流量控制 | 拥塞控制 |
|------|---------|---------|
| 目的 | 防止接收方缓冲区溢出 | 防止网络拥塞 |
| 控制对象 | 发送方与接收方之间 | 整个网络 |
| 机制 | 滑动窗口（rwnd） | 拥塞窗口（cwnd） |
| 实现位置 | 端到端 | 发送方 |

## 流量控制示例

假设接收方缓冲区大小为1000字节：

1. 初始通告窗口：1000字节
2. 发送方发送500字节数据
3. 接收方确认数据，通告窗口：500字节
4. 发送方再发送500字节数据
5. 接收方确认数据，通告窗口：0字节
6. 发送方停止发送
7. 接收方处理完数据，通告窗口：1000字节
8. 发送方继续发送

## 总结

流量控制通过滑动窗口机制，根据接收方的处理能力动态调整发送速率，确保数据的可靠传输。
''',

    '拥塞控制.md': '''# TCP拥塞控制

## 拥塞控制概述

拥塞控制是TCP的核心功能，用于防止网络因过多数据而导致拥塞。

### 拥塞的原因

1. **带宽不足**：网络带宽无法满足所有数据流的需求
2. **路由器缓冲区溢出**：路由器缓冲区有限，过多数据会导致丢包
3. **数据传输速率过高**：发送方发送速率超过网络处理能力

### 拥塞的后果

1. **丢包**：数据包丢失
2. **延迟增加**：数据包在路由器中排队等待
3. **吞吐量下降**：实际传输速率降低

## 拥塞控制机制

### 拥塞窗口（cwnd）

拥塞窗口是发送方维护的变量，表示在没有确认的情况下可以发送的最大数据量。

### 慢启动

1. 初始阶段，cwnd = 1个MSS
2. 每收到一个ACK，cwnd翻倍
3. 快速探测网络带宽

### 拥塞避免

1. 当cwnd达到ssthresh时，进入拥塞避免阶段
2. 每经过一个RTT，cwnd增加1个MSS
3. 缓慢增加发送速率

### 快速重传

1. 当收到3个重复的ACK时，认为发生了丢包
2. 立即重传丢失的段，无需等待超时

### 快速恢复

1. 检测到丢包后，不将cwnd重置为1
2. 将cwnd设置为ssthresh + 3
3. 快速恢复发送速率

## 加法增大/乘法减小（AIMD）

### AIMD策略

- **加法增大**：拥塞避免阶段，cwnd线性增加
- **乘法减小**：检测到拥塞时，cwnd减半

### AIMD的优点

1. **稳定性**：收敛到公平的带宽分配
2. **响应性**：快速降低发送速率避免拥塞

## 拥塞控制算法

### TCP Reno

Reno是最常用的TCP实现，支持：
- 慢启动
- 拥塞避免
- 快速重传
- 快速恢复

### TCP Cubic

Cubic是Linux默认的TCP实现，在高带宽网络中表现更好。

### TCP BBR

BBR是Google开发的TCP实现，基于模型的拥塞控制。

## 总结

TCP拥塞控制通过多种机制动态调整发送速率，在提高吞吐量和避免拥塞之间取得平衡。
''',

    'UDP协议详解.md': '''# UDP协议详解

## UDP概述

UDP（User Datagram Protocol）是一种无连接的传输层协议，提供不可靠的数据传输服务。

### UDP特点

- **无连接**：不需要建立和释放连接
- **不可靠**：不保证数据的可靠传输
- **面向报文**：以报文为单位传输数据
- **速度快**：开销小，传输效率高

### UDP与TCP对比

| 特性 | UDP | TCP |
|------|-----|-----|
| 连接性 | 无连接 | 面向连接 |
| 可靠性 | 不可靠 | 可靠 |
| 传输单位 | 报文 | 字节流 |
| 速度 | 快 | 慢 |
| 开销 | 小 | 大 |

## UDP报文结构

### UDP报头

1. **源端口**：2字节，发送方端口号
2. **目的端口**：2字节，接收方端口号
3. **长度**：2字节，UDP报文总长度
4. **校验和**：2字节，用于检测数据错误

### UDP数据

UDP数据部分没有固定格式，由应用层决定。

## UDP校验和

### 校验和计算

UDP校验和包括伪头部、UDP报头和数据部分。

**伪头部**：
- 源IP地址：4字节
- 目的IP地址：4字节
- 协议号：1字节（UDP为17）
- UDP长度：2字节

### 校验和验证

接收方计算校验和，如果结果不为全1，则认为数据有错误。

## UDP应用场景

### 适合UDP的场景

1. **实时应用**：如视频通话、在线游戏
2. **广播/组播**：如DNS、DHCP
3. **简单查询**：如SNMP、NTP
4. **对延迟敏感的应用**：如VoIP

### 不适合UDP的场景

1. **文件传输**：需要可靠传输
2. **数据库访问**：需要保证数据完整性
3. **邮件系统**：需要可靠传输

## UDP可靠传输

### 应用层实现可靠性

虽然UDP本身不可靠，但应用层可以实现可靠传输：

1. **确认机制**：接收方收到数据后发送确认
2. **重传机制**：发送方超时后重传未确认的数据
3. **序列号**：为每个报文分配序列号
4. **流量控制**：根据接收方能力调整发送速率

### 例子

- **QUIC**：基于UDP的可靠传输协议
- **RTP**：实时传输协议，配合RTCP实现可靠性

## 总结

UDP是一种简单高效的传输协议，适用于对实时性要求高的场景。
'''
}

for filename, content in empty_docs_content.items():
    file_path = documents_dir / filename
    if file_path.exists():
        current_content = file_path.read_text(encoding='utf-8')
        if len(current_content.strip()) == 0:
            file_path.write_text(content, encoding='utf-8')
            print(f'已填充: {filename}')
        else:
            print(f'已有内容，跳过: {filename}')
    else:
        print(f'文件不存在: {filename}')

print(f'\n共处理 {len(empty_docs_content)} 个文档')
